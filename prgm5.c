#include <stdio.h>

void main() 
{ 
    float a, b, c;
    scanf("%f %f %f", &a, &b, &c); 
  if (a>= b && a>= c) 
        printf("%f",a); 
  if (b>= a && b >= c) 
        printf("%f",b); 
 if (c >= && c >= b) 
        printf("%f",c); 
} 
